/* Type definitions */
type Page = {
	page: PageType;
};

type PageBlocks = {
	page: PageType;
};

type PageList = {
	pages: PagesType;
};

/* Export prop types */
export type PageProps = Page;

export type PageBlocksProps = PageBlocks;

export type PageListProps = PageList;
