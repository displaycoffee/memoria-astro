---
/* Scripts */
import { context } from '../context/scripts/context.ts';

/* Components */
import Container from '../layout/container/Container.astro';
import PostList from '../components/post/PostList.astro';

/* Get posts */
const posts = await context.wp.posts(12);
const options = await context.wp.themeOptions();
console.log('testing', options)
---

<Container>
	<h2>Astro + WordPress 🚀</h2>

	<PostList posts={posts} />
</Container>