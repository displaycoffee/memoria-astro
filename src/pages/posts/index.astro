---
/* Scripts */
import { context } from '../../context/scripts/context.ts';

/* Components */
import Container from '../../layout/container/Container.astro';
import PostList from '../../components/post/PostList.astro';

/* Get posts */
const site = await context.wp.site.site();
const posts = await context.wp.post.posts(site.pageSize);

/* Page title */
const pageTitle = 'Posts';
---

<Container title={pageTitle}>
	<h2>{pageTitle}</h2>

	<PostList posts={posts} />
</Container>